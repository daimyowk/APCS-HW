public class Driver{
    int i;
    for(i=last;i>0 && a[i-1]>newval;i--){
	a[i]=a[i-1];
    }
    a[i]=newval;
    //insertion sort
    public void shift
    int i;
    for (i=last;i>0 && data[i].compareTo(newvalue)>0; i--){
	data[i]=data[i-1];
    }
    data[i]=newvalue;
}
